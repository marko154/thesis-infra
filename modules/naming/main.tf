# One map for all five payload modules. Catalog change 06 (add a region)
# edits this file once; it is not copy-pasted per module.
locals {
  region_short = {
    eu-central-1 = "euc1"
    us-east-1    = "use1"
  }
}
