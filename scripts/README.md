# Scripts

Measurement and validation automation (stubs for now).

Planned scripts:

| Script | Purpose |
| --- | --- |
| `validate.sh` | `fmt`, `validate` across implementations |
| `plan.sh` | Run comparable `plan` for a deployment unit in all approaches |
| `measure-diff.py` | Git diff stats for a change experiment |
| `generate-report.py` | Aggregate raw results into reports |
